#include <stdio.h>
/**
 * main - main 
 * print different combo of 3 digits 
 * Return: 0
 */
int main(void)
{
	int i, j, k, l;

	for (i = 0; i < 1000; i++)
	{
		j = i / 100; /* hundreds */
		k = (i/ 10) % 10; /* tens */
		l = i % 10; /* singles */

		if (j < k && k < l)
	}
}
	{
			
}
